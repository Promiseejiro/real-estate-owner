import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["media.istockphoto.com", "encrypted-tbn0.gstatic.com", "img.freepik.com", "cdn.britannica.com", "res.cloudinary.com"],
  },
};

export default nextConfig;
